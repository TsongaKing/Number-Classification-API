# Force Spring Boot to listen on all interfaces (0.0.0.0)
server.address=0.0.0.0
# Use PORT from Azure environment variables, fallback to 8080 locally
server.port=${PORT:8080}

# External API configuration
fun.fact.api.url=http://numbersapi.com/